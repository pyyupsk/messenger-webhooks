{
  "name": "@repo/examples",
  "version": "1.3.0",
  "description": "A simple weather bot using @pyyupsk/messenger-webhooks",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/index.ts --watch",
    "prebuild": "rimraf dist",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pyyupsk/messenger-webhooks": "workspace:*",
    "axios": "^1.13.2",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^24.10.1",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  }
}
